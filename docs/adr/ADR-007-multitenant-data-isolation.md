# ADR-007: マルチテナントデータ分離

## ステータス
承認済み

## コンテキスト
複数組織（テナント）のデータを安全に分離する必要

## 決定
Firestoreのドキュメントパスによるデータ分離。tenants/{tenantId}/配下に全データを格納

## 根拠
物理的なデータ分離によりテナント間のデータ漏洩を防止。Firestoreのセキュリティルールとも親和性が高い

## 影響
全API呼び出しにtenantIdが必須。URLパス: /api/v2/:tenant/
